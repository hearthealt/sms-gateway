# 短信网关 · 三端校验报告

**日期**：2026-09-18
**范围**：`backend/`（Spring Boot）、`management/`（Vue 3）、`android/`（Kotlin + Compose）
**方法**：四路并行审查（后端 / 管理前端 / Android / 跨端契约），加上对**运行中后端**的只读 HTTP 探针。所有结论由我逐条打开源码复核。

> **关于可信度**：本报告每条都标注了复核状态。
> `✅ 已复核` = 我亲自打开源码（或实际发请求）确认过。
> `⚠️ 待复核` = 自动审查报出、代码片段可信，但我没有独立验证，采信前请自行确认。
> 文末单列一节「**已复核为不成立的项**」，那是审查报错、我验伪的。

---

## 摘要

| # | 问题 | 端 | 严重度 | 复核 |
|---|---|---|---|---|
| 1 | 重复短信必然触发唯一约束冲突 → 500 → 设备无限重试 | 后端 | **高** | ✅ |
| 2 | 上报/心跳信任请求体里的 `deviceId`，可越权写他人设备 | 后端 | **高** | ✅ |
| 3 | 免鉴权注册接口会**返还已存在设备的令牌** | 后端 | **高** | ✅ |
| 4 | 密钥/管理口令硬编码，且仓库自带的 compose 不注入覆盖项 | 后端+部署 | **高** | ✅ |
| 5 | 演示版 OkHttp 日志打到 BODY：令牌、短信全文、验证码进 logcat | Android | **高** | ✅ |
| 6 | 升级 APK 后网关静默停摆，且无自愈路径 | Android | **高** | ✅ |
| 7 | 排除词会把真实验证码短信整条丢弃 | Android | **高** | ✅ |
| 8 | `/api/sms/wait` 的 timeout 无上限且阻塞 Tomcat 线程 → 可打瘫整个服务 | 后端 | 中高 | ✅ |
| 9 | 每次 `/api/sms/wait` 泄漏一个永久轮询任务 | 后端 | 中高 | ✅ |
| 10 | 「测试连接」把设备令牌发往用户随手输入的地址 | Android | 中 | ✅ |
| 11 | 分页「条/页」选择器完全不生效 | 前端 | 中 | ✅ |
| 12 | 管理员令牌存 localStorage 且客户端不判过期 | 前端 | 中 | ✅ |
| 13 | Redis 无密码却明文存着管理令牌与 API Key，端口对外发布 | 后端+部署 | 中 | ✅ |
| 14 | 时区未固定：容器 UTC，开发机 CST | 后端+部署 | 中 | ✅ |
| 15 | `SmsStatus.DUPLICATE` 是三端死代码：schema/后端/前端/Android 都实现了，但永远产生不了 | 三端 | 中 | ✅ |
| 16 | `phone` / `phoneNumber` 命名分裂，靠 `@JsonAlias` 打补丁，且该注解是整批糊上去的 | 跨端 | 中 | ✅ |
| 17 | 管理端缺 OPTIONS 放行 —— 服务浏览器调用方的那个拦截器反而没做防护 | 后端+前端 | 中 | ✅ |

---

## 一、后端

### 🔴 1. 重复短信必然撞唯一约束 → 恒 500 → 设备无限重试　`✅ 已复核`

**位置**：`backend/src/main/java/com/smsgateway/service/SmsService.java:86-108` + `backend/sql/schema.sql:59`

命中「内容重复」分支时，代码新建一条记录并把 `sourceHash` 设为**与已存在行完全相同**的值：

```java
Optional<SmsMessage> existingByHash = smsMessageRepository.findBySourceHash(sourceHash);
if (existingByHash.isPresent()) {
    SmsMessage duplicateMsg = new SmsMessage();
    ...
    duplicateMsg.setSourceHash(sourceHash);   // ← 和 existingByHash 的 hash 一模一样
    smsMessageRepository.save(duplicateMsg);
```

而建表脚本给这一列建的是唯一索引：

```sql
UNIQUE KEY uk_source_hash (source_hash),
```

**这条 INSERT 100% 失败**，抛 `DataIntegrityViolationException`，被 `GlobalExceptionHandler` 兜成 500。

**触发场景**：用户点「重新发送验证码」，服务商重发的短信**逐字相同**（同一个码）→ 设备生成的 `localMessageId` 含时间戳所以不同，走不到幂等分支 → 命中 `findBySourceHash` → 500。

**为什么严重**：设备端把 500 归为 `TRANSIENT`（`SmsUploadWorker.kt:167`），于是这条短信**永久重试、永不终结**（见 #15），短信一直传不上去，还持续耗电。跨设备同样触发（两台手机收到同一段固定文案）。

**修复方向**：重复行不该复用同一个 `source_hash`（比如置空、或加 `device_id` 前缀、或改成非唯一索引），或者干脆不落这条重复记录、只记日志。

---

### 🔴 2. 上报/心跳用请求体里的 `deviceId`，而不是认证出的身份　`✅ 已复核`

**位置**：`service/SmsService.java:64-65`、`controller/SmsController.java:43-49`

拦截器已经把认证通过的设备写进了请求属性：

```java
// DeviceAuthInterceptor.java:64-65
request.setAttribute("deviceId", device.getDeviceId());
request.setAttribute("devicePk", device.getId());
```

但上报链路**完全没用它**，而是拿请求体里的 `deviceId` 重新查库：

```java
// SmsService.java:64
SmsDevice device = deviceRepository.findByDeviceId(request.getDeviceId())   // ← 请求体
```

`devicePk` 这个属性**全项目无人读取**（我 grep 确认过）—— 也就是「忘了用」，不是有意为之。

同一个 controller 里的 `GET /api/device/sms` 用法是对的，形成了刺眼的对照：

```java
// SmsController.java:55
String deviceId = (String) httpRequest.getAttribute("deviceId");   // ← 正确
```

**触发场景**：攻击者拿**自己的**合法设备令牌调 `POST /api/sms/receive`，body 里把 `deviceId` 填成受害者设备、`phone` 填成受害者号码、`content` 填任意内容 → 该短信被计入**受害者**设备名下并出现在管理后台；更糟的是 `phone` 可控意味着 `sms:code:<受害者号码>` 被写成攻击者指定的验证码并广播到 `sms:channel:*`，正在 `GET /api/sms/wait?phone=<受害者号码>` 的外部调用方**立刻拿到攻击者指定的码** —— 整条业务链路被劫持。

心跳同理，可覆写他人设备的 `deviceName` / `phoneNumber` / `battery`。

**修复**：两条链路都改成读 `request.getAttribute("deviceId")`，并忽略请求体里的 `deviceId`。

---

### 🔴 3. 免鉴权注册接口会返还已存在设备的令牌　`✅ 已复核`

**位置**：`service/DeviceService.java:65-90`、`interceptor/DeviceAuthInterceptor.java:28-31`

注册接口被拦截器整体放行：

```java
if (requestURI.equals("/api/device/register")) return true;
```

而对**已存在**的 `deviceId`，走的是「重注册」分支，把令牌当响应体返回：

```java
SmsDevice device = existingDevice.get();
String deviceToken = HashUtil.hmacSha256(deviceId, secretKey);
...
return new DeviceRegisterResponse(deviceToken, deviceId, device.getStatus());
```

**触发场景**：

```
POST /api/device/register  {"deviceId":"android-<受害者 SSAID>"}
→ 响应 data.deviceToken
→ 带 Authorization: Bearer <token> 调 GET /api/device/sms → 读到该设备全部短信与验证码
```

`deviceId` 本身不好猜（是 SSAID 派生值），但它**在管理后台的设备列表里对每个管理员可见**，也会出现在设备端界面和任何截图上。

**同一根因的另一半**：令牌 = `HMAC-SHA256(deviceId, app.secret.key)`，用的是**确定性 HMAC**，而密钥硬编码在仓库里（见 #4）。所以读过这个仓库的人可以对**任意** `deviceId` 直接算出令牌，连注册接口都不用调。

**修复方向**：注册接口对已存在的设备应要求某种证明（原令牌 / 一次性注册码），或改为服务端随机签发令牌而非确定性推导。

---

### 🔴 4. 密钥与管理口令硬编码，且仓库自带的部署路径不覆盖它们　`✅ 已复核`

**位置**：`backend/src/main/resources/application.yml:8,25,33-34`、`docker/docker-compose.yml:42-47`

```yaml
spring.datasource.password: root
app.secret.key: sms-gateway-secret-key-2024        # 令牌 HMAC 密钥
app.admin.default-password: zaq1,lp-               # 首次启动创建的管理员口令
```

三处**都能**被环境变量覆盖，但 `docker-compose.yml` 的 backend 服务只注入了这些：

```
SPRING_DATASOURCE_URL / SPRING_DATASOURCE_USERNAME / SPRING_DATASOURCE_PASSWORD
SPRING_REDIS_HOST / SPRING_REDIS_PORT
```

**没有** `APP_SECRET_KEY`，**没有** `APP_ADMIN_DEFAULT_PASSWORD`。

**触发场景**：照着仓库文档 `docker compose up` 部署 → `admin_user` 表为空 → `DataInitializer` 用 `zaq1,lp-` 建号 → **任何读过这个仓库的人用 `admin/zaq1,lp-` 登录管理后台**，可签发 API Key、删规则、禁用全部设备。同时 `app.secret.key` 公开 → 结合 #3 可伪造任意设备身份。

**附加坑**：默认账号只在 `admin_user` 表为空时创建，之后**改配置不会更新**已存在的账号 —— 就算补了环境变量，密码还是旧的。

---

### 🟠 8. `/api/sms/wait` 的 timeout 无上限，且阻塞 Tomcat 工作线程　`✅ 已复核`

**位置**：`controller/SmsController.java:91-101`

```java
@RequestParam(value = "timeout", defaultValue = "60") long timeout
...
CompletableFuture<SmsWaitResponse> future = waitingService.waitForSms(phone, timeout);
SmsWaitResponse response = future.get();      // ← 无超时参数，占住 Servlet 线程
```

`timeout` 是调用方可控的 `long`，没有限幅，也没有和 Tomcat 线程池挂钩。

**触发场景**：持有一个合法 API Key 的调用方并发发 200 个 `GET /api/sms/wait?phone=138...&timeout=86400` → Tomcat 默认 200 个工作线程**全部挂 24 小时** → 此后设备注册、心跳、短信上报、管理后台**全部无响应**，整个网关瘫掉。非恶意也够呛：并发几百个 `timeout=600` 即可复现。

**修复**：给 `timeout` 加硬上限（如 120 秒），`future.get(timeout+ε, SECONDS)`，并考虑改用异步 Servlet（`DeferredResult`）而非占线程。

---

### 🟠 9. 每次 `/api/sms/wait` 泄漏一个永久轮询任务　`✅ 已复核`

**位置**：`service/WaitingService.java:113-138`

```java
scheduler.scheduleAtFixedRate(() -> {
    if (future.isDone()) { return; }     // ← 只是提前 return，任务本身还在调度队列里
    ...
}, 0, POLL_INTERVAL_MS, TimeUnit.MILLISECONDS);
```

返回的 `ScheduledFuture` 没有被保存，`cancel()` 在全项目**没有任何调用点**（我 grep 确认）。所以每个 `wait` 请求都留下一个**永久存活**的 500ms 周期任务，持有 future、闭包和 Redis key 字符串，无法被 GC。调度器还是单线程的。

**触发场景**：按每天 1 万次 `wait` 算，一天后单线程调度器每秒要跑约 2 万个 runnable（绝大多数只做一次 `isDone()` 就返回）→ 调度线程被拖满，新请求的轮询与超时回调被饿死；同时堆持续增长。进程不重启不释放。

**修复**：保存 `ScheduledFuture`，在 future 完成时 `cancel(false)`。

---

### 🟡 13. Redis 无密码，却明文存着管理令牌与 API Key　`✅ 已复核`

**位置**：`application.yml:17-21`（无 `password`）、`docker/docker-compose.yml:24-33`、`service/AdminAuthService.java:49-51`、`service/ApiKeyService.java:97`

Redis 实例无认证，且 compose 把 `6379:6379` 发布到宿主机。而管理会话令牌的存储方式是：

```java
// AdminAuthService.java:50
redisTemplate.opsForValue().set(ADMIN_TOKEN_PREFIX + token, user.getUsername(), ttl, SECONDS);
```

API Key 更是直接当 key 名用（`sms:apikey:sk-xxx`）。

**触发场景**：任何能连到 `host:6379` 的人执行 `KEYS sms:admin:token:*` 或 `MONITOR` → 拿到一个未过期的管理令牌 → 直接调 `/api/admin/**` 完成接管，**不需要知道密码**。同理 `KEYS sms:apikey:*` 拿到全部调用方密钥。（compose 里 MySQL 的 3306 同样裸奔，root/root123。）

**修复**：Redis 设 `requirepass`、compose 里绑 `127.0.0.1:6379`、生产环境别发布端口。

---

### 🟡 14. 时区未固定　`✅ 已复核`

**位置**：`application.yml:6`（JDBC 硬编码 `serverTimezone=Asia/Shanghai`）、`backend/Dockerfile:1`（`eclipse-temurin:17-jre-alpine`，无 `TZ`）

代码里所有「今天 / 现在」都用 JVM 默认时区：`ZoneId.systemDefault()`、`LocalDate.now()`。而 Alpine 基础镜像默认 **UTC**。

**注意**：JDBC 的 `serverTimezone` 只影响 `Instant` 与时间戳的转换，**不改变 `LocalDateTime` 的字面存取**，所以它不构成补偿，反而容易让人以为已经处理过了。

**触发场景**：北京时间 00:30 收到短信 → 容器按 UTC 落库为前一天 16:30 → 管理后台列表时间早 8 小时；`DeviceService.todayStats` / `AdminDeviceService.stats` 用 UTC 的 `LocalDate.now()`，这条短信被算进**前一天**的「今日短信」。每天 08:00 前后，设备端与后台的「今日」统计会差一天。

**修复**：Dockerfile / compose 里设 `TZ=Asia/Shanghai`，或把 `LocalDate.now()` 全部改成 `LocalDate.now(ZoneId.of("Asia/Shanghai"))`。

---

### 其余后端问题（`⚠️ 待复核`，来自自动审查）

| # | 问题 | 位置 | 说明 |
|---|---|---|---|
| 7 | `sms:code:<phone>` 只写不删，5 分钟内重复 `wait` 会拿到**上一次的旧码** | `WaitingService.java:83-89` | 命中缓存即返回，不判断新鲜度，调用方无法察觉 |
| 10 | `status` 读-改-写竞态会把「禁用」覆盖回 ACTIVE | `DeviceService.java:112-137` | 心跳 merge 整行实体（无 `@DynamicUpdate`），UPDATE 写回所有列含 `status`。窗口毫秒级，批量禁用时概率上升 |
| 11 | `pageSize` 在管理端/设备端无限幅 | `AdminSmsController.java:28,46`、`DeviceController.java:51` | 只有对外接口 `/api/sms/list` 做了 `min(...,100)`；`GET /api/device/sms?pageSize=50000000` 可拉爆内存 |
| 12 | `sms_message` 只增不删 + 前导通配 LIKE + 深分页 | `SmsMessageRepository.java:30-45` | 无任何清理任务；`like '%phone%'` 使索引失效 |
| 13 | `sms_device.device_token` 无索引，却是每个设备请求的必经查询 | `sql/schema.sql:21-39` | 设备数上万后全表扫描拖慢同库写入 |
| 14 | 验证码明文进 INFO 日志 | `SmsService.java:173-174` | `log.info("... code={}", ...)` |
| 15 | DTO 无 `@Size`，超长字段走 DB 报错 → 500 而非 400 | `SmsReceiveRequest.java`、`DeviceRegisterRequest.java` | 与 `sender VARCHAR(100)` 等列宽不匹配；设备端把 500 当可重试 → 永久重试 |
| 16 | `Idempotency-Key` 请求头被接收但**完全忽略** | `SmsController.java:43-44` | 客户端确实在发这个头，静默失效 |
| 17 | `setIfAbsent` 返回值算了不用，并发去重守卫是死代码 | `SmsService.java:87` | 并发重复上报仍会撞唯一约束 |
| 18 | `timeout` 传负值 → Redis TTL 非正 → 500（应为 400） | `WaitingService.java:97` | `?timeout=-100` |
| 19 | `/api/health` 免鉴权且每次占一条 DB 连接 | `HealthController.java:64-71` | 数据库变慢时大量并发探活会把连接池占满（注：这个端点是本次会话新加的） |
| 20 | CORS `allowedOrigins("*")` | `WebConfig.java:14-17` | 鉴权走 Header 而非 Cookie，当前风险有限；一旦引入 Cookie 会话即变可利用 |

---

## 二、管理前端

### 🟠 11. 分页「条/页」选择器完全不生效　`✅ 已复核`

**位置**：`management/src/views/DeviceList.vue:80-87,106`、`SmsList.vue:127,151,226-229`

```html
<el-pagination v-model:current-page="page" :page-size="pageSize" @size-change="loadData" />
```

`pageSize` 是**单向绑定**，而 `const pageSize = ref(10)` 在全文件**没有任何写入点**（我 grep 确认：只有 `:page-size="pageSize"` 和 `pageSize: pageSize.value` 两处出现）。`loadData()` 也不接参数，所以 `@size-change` 传来的新值被丢弃。

**触发场景**：设备管理页选「50 条/页」→ 下拉框立即弹回「10 条/页」，列表内容不变。短信记录页更迷惑：`handleSizeChange()` 会把页码重置到第 1 页，用户看到「选了 50，页跳回第 1 页但仍只有 10 条」。

**修复**：改成 `v-model:page-size="pageSize"`。

---

### 🟠 12. 管理员令牌明文存 localStorage，客户端不判过期　`✅ 已复核`

**位置**：`api/http.ts:19`、`router/index.ts:66`、`views/Login.vue:128`

```ts
const token = localStorage.getItem('token')
if (token) config.headers.Authorization = `Bearer ${token}`
```

路由守卫**只判存在性**，不判有效性；服务端 TTL 是 2 小时，前端没有任何到期时间记录。

**触发场景**：管理员在公用电脑登录后关掉浏览器 → 令牌仍在 localStorage，**2 小时内任何人打开该浏览器都能以管理员身份进入**，且看不到任何提示。

当前 `src/` 里没有 `v-html`、没有把后端数据塞进 `innerHTML`（我确认过），所以**暂无可利用的注入点** —— 属潜在风险。一旦将来引入富文本或第三方 SDK，这就是可直接外带的凭证。可换 `sessionStorage` 或加本地过期时间戳。

---

### 其余前端问题（`✅ 已复核` 的两条我验过代码，其余来自自动审查）

| # | 问题 | 位置 | 复核 |
|---|---|---|---|
| 2 | 设备详情加载失败后**全屏 loading 永不消失**，且错误文案写死为「设备不存在」 | `DeviceDetail.vue:2,152-159` | ✅ `v-loading="!device"` 用「数据为空」当加载态，没有错误态；`catch { ElMessage.error('设备不存在') }` 无视真实错误 |
| 3 | 规则表单无长度校验，超 DB 列宽 → 500 | `RuleManagement.vue:75-107` | ⚠️ 同项目 API 密钥页写了 `maxlength="100"`，说明是漏配 |
| 4 | 列表请求无取消，快速翻页被旧响应覆盖 | `DeviceList.vue:124-140`、`SmsList.vue:188-208` | ⚠️ 连点页码 2、3 → 2 的响应后到并覆盖表格，分页控件却停在第 3 页 |
| 5 | 无效路径整页空白（无 catch-all 路由） | `router/index.ts:5-63` | ⚠️ 守卫因 `to.meta` 为空而放行，`router-view` 无匹配组件 |
| 6 | `deviceId` 拼进路由未编码 | `DeviceList.vue:159` 等 | ⚠️ 同仓库 `DeviceDetail.vue:63` 编码了，此处不一致 |
| 8 | 错误提示重复且文案与真实错误不符 | `DeviceDetail.vue:157` 等 | ✅ 拦截器已统一弹错，组件 catch 又弹一条写死的 |
| 9 | 登录页回车可重复提交 | `Login.vue:51,119` | ✅ `@keyup.enter` 挂在 form 上，`handleLogin` 无 `if (loading) return`，按钮的 disabled 管不到键盘 |
| 10 | `navigator.clipboard` 在 http 部署下不可用，「复制验证码」必失败 | `SmsList.vue:179` 等 | ✅ 无 `execCommand` 兜底。依赖部署方式——用 IP 访问即触发，开发时 localhost 测不出来 |
| 11 | 仪表盘 `Promise.all` 一个失败则整页数字归零 | `Dashboard.vue:342-355` | ⚠️ 任一接口超时 → 显示「总设备 0」，用户误以为真没设备 |
| 12 | 仪表盘把 `DISABLED` 当成「离线」 | `Dashboard.vue:201-203` | ✅ 后端 `toView` 产出 `online`/`offline`/`DISABLED` 三态，前端这里只判 `=== 'online'` 二值 → 已禁用设备的圆点显示为红色「离线」 |
| 13 | 接口文档页的滚动高亮很可能从未生效 | `ApiDocs.vue:36,406-431` | ⚠️ scroll 事件绑在 `.docs-content`，但真实滚动者是外层 `.app-main` |

---

## 三、Android

### 🔴 5. 发布版把 OkHttp 日志打到 BODY　`✅ 已复核`

**位置**：`app/src/main/java/com/smsgateway/app/network/RetrofitClient.kt:94-97`

```kotlin
val loggingInterceptor = HttpLoggingInterceptor().apply {
    level = HttpLoggingInterceptor.Level.BODY      // 没有任何 BuildConfig.DEBUG 判断
}
```

而 `AuthInterceptor` 会往每个请求加这些头：

```kotlin
requestBuilder.addHeader("Authorization", "Bearer $token")
requestBuilder.addHeader("X-Device-Id", deviceId)
```

**触发场景**：设备开启 USB 调试后 `adb logcat`，或用任何能读日志的工具/抓 bug report → **直接拿到设备令牌**（可冒充该设备上报任意短信，结合后端 #2 还能写他人设备）**以及当天所有短信正文与验证码明文**。

**修复**：`level = if (BuildConfig.DEBUG) BODY else NONE`。

---

### 🔴 6. 升级 APK 后网关静默停摆，且无自愈路径　`✅ 已复核`

**位置**：`app/src/main/AndroidManifest.xml:61-68`、`DashboardViewModel.kt:698-702`

覆盖安装 APK 会连带杀掉进程与前台服务。而服务**只有三个启动入口**（我 grep 确认）：

```kotlin
// DashboardViewModel.kt:701   ← 由 registerDevice() 成功路径 (:402) 与
//                               toggleService() (:688) 调用
GatewayForegroundService.start(app)
// BootReceiver.kt:21          ← 开机广播
```

清单里**没有声明 `ACTION_MY_PACKAGE_REPLACED`**（我 grep 确认为 0 次命中），`MainActivity.onCreate` 也不自动拉起服务（只申请权限 + `setContent`）。

**触发场景**：给 100 台设备推一次版本更新 → **全部设备的网关停止上报短信**，且不会自愈。界面上只有用户主动打开 App 并手动点「启动网关」才能恢复。设备无人值守时等于集体失联。

**修复**：清单里给 BootReceiver 加 `<action android:name="android.intent.action.MY_PACKAGE_REPLACED" />`（这是官方为此场景提供的豁免入口，可在后台启动前台服务）。

> 这条对本次会话尤其相关 —— 我们刚重建并覆盖安装过好几次。

---

### 🔴 7. 排除词会把真实验证码短信整条丢弃　`✅ 已复核`

**位置**：`app/src/main/java/com/smsgateway/app/parser/SmsFilter.kt:11-18`

```kotlin
private val excludeKeywords = listOf("余额", "交易", "支付", "账单", "bank")

fun shouldCollect(sender: String, body: String): Boolean {
    val text = "$sender $body".lowercase()
    if (excludeKeywords.any { text.contains(it.lowercase()) }) return false   // ← 先整条否掉
    return includeKeywords.any { text.contains(it.lowercase()) }
}
```

排除词匹配的是 `"$sender $body"` **全文**，且在任何精度判断之前先整条否掉。

**触发场景**：

- 「【支付宝】您的验证码是 123456，请勿泄露」→ 含「支付」→ **丢弃**
- 「【工商银行】验证码 123456」→ 含「bank」… 中文名不含，但英文发送方名含 `bank` 的整条丢弃
- 「【XX银行】验证码 123456，账户余额变动…」→ 含「余额」→ **丢弃**

用户就在等这条验证码，网关这边**什么都不做**，而且队列页里看不到任何痕迹（根本没入库），排查时完全无线索。

**修复方向**：排除词只应作用于**已经命中验证码特征**的短信（即先判 include、再用 exclude 做降噪），而不是独立的前置否决。

---

### 🟠 10. 「测试连接」把设备令牌发往用户随手输入的地址　`✅ 已复核`

**位置**：`RetrofitClient.kt:126-132,165`、`AuthInterceptor.kt:22-31`

```kotlin
private fun getProbeClient(): OkHttpClient =
    getHttpClient().newBuilder()          // ← getHttpClient() 已装配 AuthInterceptor
        .connectTimeout(...)...
```

所以 `probe(用户输入的 url)` 会把当前的 `Authorization: Bearer <token>` 与 `X-Device-Id` **先发出去**，「是不是本服务」的校验发生在请求之后。

**触发场景**：现场人员在设置页把地址敲成同一局域网里另一台机器（同事的开发机、打印机 Web 页、路由器管理页）后点「测试连接」→ 对方服务器日志里出现 `Authorization: Bearer <device_token>`。明文 HTTP 让同网段任何抓包方都能直接看到。

> **这条是本次会话我新写的代码引入的。** 修法：探活客户端不应挂 `AuthInterceptor`（health 端点本来就不需要鉴权）。

---

### 🟡 其余 Android 问题（`✅ 已复核` 的两条我验过，其余来自自动审查）

| # | 问题 | 位置 | 复核 |
|---|---|---|---|
| 1 | `BootReceiver` 导出且无权限保护，`QUICKBOOT_POWERON` **不在**前台服务后台启动豁免名单内 | `AndroidManifest.xml:61-68`、`BootReceiver.kt:14-24` | ✅ 代码确认：该 receiver `exported="true"` 无 `android:permission`，收到广播即 `startForegroundService`。豁免名单部分来自自动审查引用的官方文档，**平台行为我未实测** ——（a）任意第三方应用可发这个广播触发崩溃；（b）部分 ROM 开机只发 QUICKBOOT 不发 BOOT_COMPLETED 时网关静默不启动 |
| 4 | 401 无恢复路径，界面还报成「连接中断」 | `SmsUploadWorker.kt:104-108`、`HeartbeatSender.kt:62-75` | ⚠️ 令牌失效后心跳恒 401、上传直接 `Result.success()` 停车；没有任何地方提示「需重新注册」或清理令牌。而「测试连接」走的是免鉴权的 health，会一路显示绿 → 用户反复查网络 |
| 6 | 本地队列表无界增长：`deleteOldRecords` 从未被调用 | `SmsQueueDao.kt:69` | ✅ 定义在、调用点无（我 grep 确认）。已上传的行只有用户手动点「清理」才删；每行存着短信全文与验证码明文 |
| 7 | `allowBackup=true`：令牌与短信库进云备份 | `AndroidManifest.xml:34` | ⚠️ 无 `dataExtractionRules`/`fullBackupContent`，`sms_gateway_prefs.xml` 与 `sms_gateway.db` 默认在备份集合内 |
| 8 | 全局放开明文 HTTP，与扫码导入叠加是一条完整配置劫持链 | `res/xml/network_security_config.xml:13` | ⚠️ `cleartextTrafficPermitted="true"` 对所有主机生效；QR 导入只做格式校验、不校验来源 |
| 11 | 申请了 `READ_SMS` 但全项目从未读取短信 | `AndroidManifest.xml:6` | ⚠️ 功能上只需 `RECEIVE_SMS`；上架会触发 Google Play 短信权限政策审查 |
| 12 | 权限申请写在 `onCreate`，旋转屏重复弹窗 | `MainActivity.kt:60-64,85-102` | ⚠️ |
| 13 | `localMessageId` 无唯一索引，`OnConflictStrategy.IGNORE` 形同虚设 | `SmsQueueEntity.kt:6-20` | ⚠️ 唯一约束只有自增 `id`，而插入从不提供它 |
| 14 | 二维码生成跑在组合期主线程 | `QrConfigScreen.kt:67-70` | ⚠️ 720×720 矩阵 + 约 2MB Bitmap |
| 16 | `RetrofitClient` 静态配置是「检查-后-写入」，`getApiService()` 有 NPE 窗口 | `RetrofitClient.kt:33-83,134-145` | ⚠️ 自动审查自己标为**疑似**，未构造出复现 |
| 17 | 心跳每 30 秒无谓写一次 SharedPreferences | `DeviceStatus.kt:35-41` | ⚠️ 无条件 `edit().apply()`，7×24 运行每天约 2880 次无变化落盘 |

---

## 四、跨端契约

### 🟠 1. `phone` / `phoneNumber` 命名分裂，靠 `@JsonAlias` 打补丁掩盖　`✅ 已复核`

**位置**：`DeviceRegisterRequest.java:23-24`、`HeartbeatRequest.java:22-23`（后端）；`DeviceInfo.kt:8`、`HeartbeatRequest.kt:14`（Android）

注册与心跳两个入参 DTO 用 `phoneNumber`，Android 发 `phone`：

```java
/** Android 客户端字段名为 phone，此处兼容两种写法，
 *  否则手机号会因字段名不匹配而始终为 null。 */
@JsonAlias("phone")
private String phoneNumber;
```

**真正写错的是后端**：它自己的其余 5 个契约（`SmsReceiveRequest`、`DeviceView`、`SmsView`、`SmsWaitResponse`、`ClientSmsView`）**全部叫 `phone`**，只有这两个 DTO 把内部列名（`phone_number`）泄漏到了 API 层，再用别名抹平。

**补丁痕迹**：同文件里 `@JsonAlias("appVersion")` 加在**本来就叫** `appVersion` 的字段上 —— 这是个彻底的 no-op，说明这批注解是整批糊上去的、没逐个核对。

**触发场景**：一旦有人删掉这两处 `@JsonAlias`（它看起来像冗余注解），`request.getPhoneNumber()` 静默为 `null` → `DeviceService.java:76-78,122-124` 的判空直接跳过 —— 不报错、不写日志，管理后台设备列表的「手机号」列永远是 `-`。

---

### 🟠 2. 管理端缺 OPTIONS 放行，而客户端接口有　`✅ 已复核`

**位置**：`AdminAuthInterceptor.java:28-38` vs `ClientAuthInterceptor.java:33-37`

`ClientAuthInterceptor` 显式放行预检，还写了注释说明原因：

```java
// 浏览器预检请求不带 Authorization 头（规范如此，浏览器不会给它加自定义头），
// 拦下来会让所有浏览器端调用方卡在 401，真实请求根本发不出去。
if (HttpMethod.OPTIONS.matches(request.getMethod())) return true;
```

而**真正服务浏览器调用方的** `AdminAuthInterceptor` **没有**这段 —— 它上来就查 `Authorization` 头。

**触发场景**：仅当管理后台与后端**不同源**时踩到。浏览器对 `PUT /api/admin/device/{id}/enabled` 先发 OPTIONS → 被拦成 401 → 预检失败 → 真实请求根本不发出，**管理端所有写操作全废**。开发期走 vite 代理是同源、生产用 nginx 反代同源都不触发；但 `WebConfig` 明确配了 `allowedOrigins("*")`，说明跨源是被支持的用法，这条路径没有防护。

写法上的不对称很刺眼：**服务非浏览器调用方的那个拦截器做了防护，服务浏览器调用方的那个反而没做。**

---

### 🟡 3. 校验规则三处都不设上限，唯独 DB 层会拒绝　`✅ 已复核`

**位置**：`RuleManagement.vue:147-151`（前端）、`CollectRuleRequest.java:17-23`（后端）、`schema.sql:73-77`（DB）

前端 `formRules` 只有 `required` 没有 `max`，后端 DTO 只有 `@NotBlank` 没有 `@Size`，只有 DB 列宽是硬约束（`rule_name VARCHAR(100)`、`sender_pattern VARCHAR(255)`）。

**触发场景**：管理员在「规则名称」贴一段超 100 字符的文本（正则匹配模式很容易写超 255）→ `DataIntegrityViolationException` → 500「服务器内部错误」。管理员会往服务端故障方向排查，而实际只是名字太长。同项目的 API 密钥页却规规矩矩写了 `maxlength="100"`，说明这是漏配而非有意。

---

### 🟡 4. 客户端发了后端根本不读的字段与请求头　`✅ 已复核`

| 客户端在发 | 后端 | 后果 |
|---|---|---|
| `X-Device-Id`、`X-Timestamp` 请求头 | 三处拦截器**只读** `Authorization` | 纯无效流量。设备归属由令牌反查，`X-Device-Id` 与令牌不一致时以令牌为准 |
| `Idempotency-Key` 请求头 | `SmsController.java:43` 收下，`SmsService.java:63` **方法体内从未引用** | 真正的幂等键是 `(deviceId, localMessageId)`。按文档依赖此头的调用方会得到「不生效」的结果 |
| `timestamp` 字段（心跳） | `HeartbeatRequest` DTO 里**没有这个字段** | 当前被 Jackson 静默丢弃（`FAIL_ON_UNKNOWN_PROPERTIES=false`）。但客户端发的是**带时区偏移**的 ISO 串，而后端其余时间字段都是无偏移 `LocalDateTime` —— 哪天后端补上这个字段，每次心跳都会 400 |

---

### 🟡 5. `receiveTime` 的类型契约不一致（当前靠强制转换没炸）　`✅ 已复核`

Android 侧 `SmsUploadRequest.receiveTime` 声明为 **`String`**（`SmsUploadRequest.kt:10`，`SmsUploadWorker.kt:157` 用 `sms.receiveTime.toString()`），后端声明为 **`Long`**（`SmsReceiveRequest.java:34`）。

单位语义（epoch 毫秒）是一致的，序列化成 `"1758100000000"` 后靠 Jackson 默认的字符串→数字强转才没炸。**当前功能正常**，但这是类型层面的契约不一致 —— 将来若开启 `MapperFeature.ALLOW_COERCION_OF_SCALARS=false`，**所有短信上报立刻 400**。

---

### 🔵 6. 其余契约瑕疵（`⚠️ 待复核` 或影响很小）

| # | 问题 | 位置 |
|---|---|---|
| 6a | 管理端 `Device.status` 联合类型含后端**永不下发**的取值（`ACTIVE`/`INACTIVE`），是照 schema 注释写的；后端实际做的是 `online`/`offline`/`DISABLED` 三态展示值 | `types/index.ts:12` vs `AdminDeviceService.java:77-80` |
| 6b | `SmsView.isRead` 的 `@JsonProperty("isRead")` 疑似**叠加了一个新字段**而非修正名字 —— Jackson 由 `isRead()` 推导出的属性名是 `read`，可能与显式指定名各自成一个属性。前端读 `row.isRead` 所以不影响功能。**可打一次 `GET /api/admin/sms/list` 看原始 JSON 是否含 `read` 键确认** | `SmsView.java:29-34` |
| 6c | 两个拦截器放行登录路径的写法不一致（`endsWith` vs `equals`）。自动审查尝试构造绕过未成功（Spring 6 `PathPattern` 下无可达 handler），仅写法不一致 | `AdminAuthInterceptor.java:30` vs `DeviceAuthInterceptor.java:29` |

---

### ✅ 逐项核对、确认无误的部分

以下几点是自动审查**主动核对过并确认没问题**的，列出来是为了让你知道覆盖面（不是没查，是查了没事）：

- **接口路径与方法**：Android 的 5 个设备端调用 + `/api/health` 探活，管理端的 18 个调用，与后端各 Controller 的映射**逐一对上**。**没有客户端在调不存在的接口。**
- **成功判定基准**：后端 12 处 `ApiResult.error(...)` **全部**与 HTTP 状态码配对，不存在「HTTP 200 但 code 非 200」。因此管理端看 `body.code === 200` 与 Android 看 `response.isSuccessful` 在现状下**等价**，不会把失败当成功。
  > 但这是一处**约定而非机制** —— 靠"所有人都记得用 `ResponseEntity.status`"这条纪律撑着。若将来有人写 `return ResponseEntity.ok(ApiResult.error(...))`，设备端就会把失败当成功、**静默丢弃短信**。建议补一个测试守住它。
- **鉴权头**：三端统一 `Authorization: Bearer <token>`。
- **分页**：请求参数一律 `page`/`pageSize`（无变体），响应一律 `PageResult{records,total,page,pageSize}`。
- **枚举字面量**：短信状态、设备禁用态三端一致；Android 本地的 `pending/uploaded/failed` 只存在于 Room 库、从不上报，与后端 `SmsStatus` 无交集。
- **时间入参格式**：管理端 `value-format="YYYY-MM-DD HH:mm:ss"` 对齐后端 `@DateTimeFormat`；API 密钥的 `YYYY-MM-DD[T]HH:mm:ss` 对齐 `LocalDateTime`。

---

### 🔗 与后端 #1 的呼应

跨端审查**独立**确认了后端那个「重复短信 100% 失败」的 bug，并补了两个很有价值的角度：

1. **`SmsStatus.DUPLICATE` 是三端的死代码** —— schema、后端枚举、管理端的「重复」标签（`SmsList.vue:111`）、Android 的「内容重复」标签（`MainActivity.kt:937-941`）**全都实现了，但永远不可能产生**。
2. **测试把这条分支绕过去了**：`SmsServiceTest.java:76` 用 `when(smsMessageRepository.findBySourceHash(anyString())).thenReturn(Optional.empty())` 把查询**恒桩成"查不到"**，所以重复分支从未被执行过。这解释了它为什么能存活至今。

同处 `SmsService.java:87` 的 `Boolean isNewDedup = redisTemplate...setIfAbsent(...)` 赋值后从未被读取，Redis 侧去重**实际未参与任何分支**。

> 另：管理前端的 `PageResult` 字段名、`SmsView.isRead` 的 `@JsonProperty` 对齐、短信列表的时间格式（`YYYY-MM-DD HH:mm:ss` ↔ `@DateTimeFormat`）、`status` 枚举、API 密钥 `expiresAt` 的 ISO 格式 —— 自动审查逐项比对过，**均对得上**。

---

## 五、已复核为「不成立」的项

审查报出、我验伪的，列出以免误改：

| 报出的问题 | 实际 |
|---|---|
| 「设置页的一次性提示会复现：清理记录后立刻返回，再进设置页又弹一次」 | **不成立（对 `settingsMessage`）**。`toast()` 是 `scope.launch { showSnackbar() }`，**立即返回**，紧随其后的 `clearSettingsMessage()` 同步执行，状态当场清空，不随页面离开而残留。（该问题对 `registerError` 也不成立 —— 它的 `LaunchedEffect` 在 MainActivity 顶层，Activity 存活期间不会被取消。） |
| 「`clearUploadedRecords()` 的结果哪儿都不显示」 | **不成立**（这是我在本次会话中先犯的错）。它的按钮在设置页「数据」卡片（`MainActivity:690`），消息原本渲染在设置页。是**我上一版删掉那行内联文字时把它的显示位置一并弄没了** —— 现已改为走 `settingsMessage` → snackbar。 |

---

## 六、建议的修复顺序

**第一批（会造成数据丢失或服务不可用，建议立刻）**

1. **后端 #1** 重复短信 500 + 无限重试 —— 修唯一约束或去掉重复落库
2. **后端 #2** 越权写他人设备 —— 改用 `request.getAttribute("deviceId")`
3. **后端 #4 + #13** 密钥/口令/Redis —— 部署前必须覆盖，否则等于把管理后台和所有设备令牌公开
4. **Android #6** 加 `MY_PACKAGE_REPLACED` —— 否则推一次版本更新全车队失联
5. **Android #7** 排除词吞掉真实验证码 —— 这是产品核心功能失效
6. **Android #5** 关掉发布版的 BODY 日志

**第二批（影响可用性 / 可被滥用）**

7. **后端 #8、#9** `wait` 的超时限幅 + 轮询任务泄漏
8. **后端 #3** 注册接口返还令牌 —— 需要设计取舍（可能要改令牌签发方式）
9. **Android #10** 探活别带鉴权头（本次会话引入）
10. **前端 #11、#12** 分页失效 + 令牌存储

**第三批（工程质量）**

11. 后端 #12、#13 的索引与清理任务；#15 补 `@Size`；#16 `Idempotency-Key` 要么实现要么删掉
12. Android #4 的 401 恢复路径；#6 的本地表清理
13. 前端 #2、#4、#9、#10、#12

---

*审查方式：四路并行代码审查 + 对运行中后端的只读 HTTP 探针。所有 `✅ 已复核` 条目均由我打开源码或实际发请求确认；`⚠️ 待复核` 条目来自自动审查，代码位置可信但未独立验证。*
