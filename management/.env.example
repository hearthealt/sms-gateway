# 复制成 .env 后按需修改。只有 VITE_ 前缀的变量会暴露给前端代码。

# 设备要访问的服务器地址，会写进「快速连接」与「设备恢复码」的二维码里。
# 不配的话这两个功能会直接报错、拒绝生成二维码 —— 见下。
#
# 不配的话退回当前访问者的 origin —— 开发时那是 localhost:5173，
# 对手机毫无意义（vite 只是把 /api 代理到 localhost:8080）。
# 本机联调时填局域网地址，例如：
# VITE_DEVICE_SERVER_URL=http://192.168.1.100:8080
