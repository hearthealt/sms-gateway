# 复制成 .env 后按需修改。只有 VITE_ 前缀的变量会暴露给前端代码。

# 设备要访问的服务器地址，会写进「快速连接」与「设备恢复码」的二维码里。
#
# **这个文件只影响 `npm run dev`。** 正经部署时这个值取「管理员访问后台用的那个
# origin」（前端是唯一入口），不读这里 —— 见 src/api/device.ts。
#
# 本机联调时填局域网地址：不配的话开发环境会明确报错、拒绝生成二维码
# （此时 origin 是 localhost:5173，而 localhost 对手机来说指向它自己）。
# VITE_DEVICE_SERVER_URL=http://192.168.1.100:8080
